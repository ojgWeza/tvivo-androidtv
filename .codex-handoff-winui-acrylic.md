New instructions from the user (previous investigation/fix attempts may not have landed - App.xaml currently still just has <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" /> as the only merged dictionary, no app-owned brushes exist yet).

Task: Fix the WinUI 3 unpackaged app error where `AcrylicBackgroundFillColorDefaultBrush` cannot be resolved.

Project: windows-spike/src/Tvivo.App (Tvivo.App.csproj: net9.0-windows10.0.19041.0, WindowsPackageType=None, WindowsAppSDKSelfContained=false, UseWinUI=true, Microsoft.WindowsAppSDK 2.5.1).

Hard requirements:
- Keep the app unpackaged. Do NOT convert to MSIX.
- Inspect App.xaml, App.xaml.cs, MainWindow.xaml, the csproj/app.manifest, and every place a theme resource like AcrylicBackgroundFillColorDefaultBrush could be pulled in implicitly (default Button/control styles via XamlControlsResources, ThemeResources, Windows App SDK bootstrap).
- Determine the REAL root cause: is it Windows App SDK bootstrap/init ordering, resource dictionary merge ordering, XamlControlsResources itself, or an unsafe app dependency on a WinUI framework resource that isn't guaranteed to exist unpackaged? Do not guess - verify by reading source/docs and reproducing.
- Do NOT blindly add XamlControlsResources back if its initialization is what's throwing.
- Fix the root cause with the smallest architectural change possible.
- Where the app (not framework controls) directly depends on AcrylicBackgroundFillColorDefaultBrush, replace with an app-owned semantic resource, e.g. AppPanelSurfaceBrush, defined in App.xaml resources.
- Keep whole-window backdrop (SystemBackdrop / Mica / Acrylic) logic separate from UI surface brushes - don't conflate the two.
- Do not touch playback architecture (Tvivo.Playback, WindowsPlaybackEngine, VlcPlaybackEngine) and don't add new libraries.
- After the fix, BUILD and RUN the actual unpackaged executable (dotnet run or dotnet build + run the exe directly, not just a package/msix flow) and confirm it launches without the resource-resolution exception. Compilation succeeding is not sufficient - must actually launch and stay up.
- After the fix is verified working, grep the whole windows-spike tree for any remaining references to AcrylicBackgroundFillColorDefaultBrush and report what's left (framework-internal references are fine and expected; app-code references should be gone).

When done, report back concisely: (1) root cause, (2) files changed, (3) exact fix applied, (4) build/runtime result (paste key success/failure evidence, e.g. log tail from tvivo-launch.log or console output), (5) grep results for AcrylicBackgroundFillColorDefaultBrush.
