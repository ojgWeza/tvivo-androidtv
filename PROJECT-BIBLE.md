# Tvivo Project Bible

The existing `CLAUDE.md`, `AGENTS.md`, `README.md`, and `TODOS.md` are authoritative. This file is the compact index; load the relevant `docs/` detail file rather than the whole project history.

Shared rules: `D:\HCode\DEV-BIBLE.md`.
