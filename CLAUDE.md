# Tvivo CLAUDE.md index

This is the compact entry point. Original content was split into bible-detail\claude-*.md after exact SHA-256 reconstruction verification.

At every new session, read D:\HCode\DEV-BIBLE.md and todo-tree\00-overview.md first, then load only the relevant detail files.

**TODO/bible maintenance is binding, not optional**: `D:\HCode\bible-detail\todo-and-bible-maintenance.md`.
`todo-tree/` holds open items only (index in `todo-tree\01-open.md`); closed work and its
lessons live in `docs\decisions.md`. Do not let either drift back into a dated-narrative
dump — trim it the same way, with the same fail-proof verification, the next time it jams up.

**Division of labor with Codex is binding, not per-session guidance**:
`bible-detail\claude-07-agent-division.md`. Read it at session start — inside or outside
Herdr — and self-initialize per its "Session-start self-check": check for an existing
Codex pane before starting one, pick up work directly from `todo-tree\01-open.md`, and
follow the plan → Codex-review → execute → Codex-test → close loop without waiting for
the user to re-paste the brief.

## Detail files — two axes, not duplicates

`claude-*.md` is the engineering bible (build state, non-negotiable technical constraints,
build order, testing, conventions). `product-*.md` is the product spec (users, positioning,
operating context, product principles, brand, accessibility) generated by the
`impeccable:product-schema`. They're filed side by side because they answer different
questions — "how is this built" vs "what is this for" — not because content was copied
between them. Where they used to restate the same technical constraint twice
(`product-06.md` capabilities), the product file now points at `claude-03.md` instead of
repeating it; keep it that way rather than re-duplicating on the next edit.

- bible-detail\claude-00.md
- bible-detail\claude-01.md
- bible-detail\claude-02.md
- bible-detail\claude-03.md
- bible-detail\claude-04.md
- bible-detail\claude-05.md
- bible-detail\claude-06.md
- bible-detail\claude-07-agent-division.md — Claude/Codex division of labor (binding, see above)
- bible-detail\product-00.md .. product-10.md — product spec, see above
